"""
Cross-broker instrument mapping.

This package reads the raw broker instrument tables populated daily by ``data/instruments/`` and maps every row into the unified tables ``instruments.master`` and ``instruments.broker_mappings``.
"""